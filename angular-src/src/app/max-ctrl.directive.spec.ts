import { MaxCtrlDirective } from './max-ctrl.directive';

describe('MaxCtrlDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxCtrlDirective();
    expect(directive).toBeTruthy();
  });
});
